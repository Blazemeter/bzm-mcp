[project]
name = "bzm-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.13.0",
    "pyinstaller>=6.0.0",
]

[project.scripts]
bzm-mcp = "main:run"
